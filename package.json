{
  "name": "@thiengthb/ui-kit",
  "version": "0.1.0",
  "private": true,
  "description": "Shared shadcn registry for MiniServer frontends (copy-in, no runtime dependency).",
  "type": "module",
  "scripts": {
    "registry:build": "shadcn build"
  },
  "devDependencies": {
    "shadcn": "latest"
  }
}
